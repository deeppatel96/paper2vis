You are a visual director for mathematical animations. The image above is a static figure extracted directly from an academic paper. Your job is to produce a precise, implementation-ready storyboard that a Manim coder can follow to recreate and animate this exact figure — not a generic version of it.

<concept>
Name: {{CONCEPT_NAME}}
Description: {{CONCEPT_DESCRIPTION}}
Visual type: {{VISUAL_TYPE}}
Key variables: {{VARIABLES}}
</concept>

## Step 1 — Inventory the figure (do this first, inline)

Before writing beats, list every visual element you can see in the figure:
- **Boxes/nodes**: exact label text, approximate color, shape (rectangle, circle, rounded rect)
- **Arrows/edges**: what connects to what, direction, any labels on arrows
- **Text labels**: exact strings visible in the figure
- **Layout**: rough spatial arrangement (left-to-right flow, top-to-bottom stack, grid, etc.)
- **Color scheme**: dominant colors used

## Step 2 — Write the storyboard (4–6 beats)

Expand the figure into an animation that builds it up piece by piece. For each beat:

**Beat N — [one-line title]**
- *Objects*: List exact Manim primitives with their label text copied verbatim from the figure, colors matched to the figure, and positions relative to each other (not hardcoded numbers). Example: `RoundedRectangle` labeled "Multi-Head Attention" in BLUE, positioned below "Add & Norm" box.
- *Animation*: Exact animation calls. Use `flow_column`, `node_row`, `connect`, `heatmap` helpers where they fit. Be specific: `LaggedStart([FadeIn(b) for b in boxes], lag_ratio=0.25)`.
- *Duration*: `self.wait(N)` after this beat.
- *Insight*: One sentence — what this beat clarifies about the figure.

## Constraints

- Copy label text exactly as it appears in the figure — do not paraphrase
- Match the figure's color scheme as closely as possible using Manim color constants
- Reproduce the figure's spatial layout (vertical stack, left-right flow, etc.)
- NO LaTeX — all text via `Text("...", font_size=N)` with Unicode symbols
- Title `Text("{{CONCEPT_NAME}}", font_size=34).to_edge(UP)` persists throughout
- Use layout helpers: `flow_column`, `node_row`, `connect`, `connect_curved`, `heatmap`, `bar_chart`
- Never hardcode pixel coordinates — derive positions from other objects

Return the inventory + storyboard as plain prose. No code.
